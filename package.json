{
  "name": "trip-weather-planner",
  "private": true,
  "type": "module",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"bun run dev:backend\" \"bun run dev:frontend\"",
    "dev:backend": "bun run --cwd backend dev",
    "dev:frontend": "bun run --cwd frontend dev",
    "lint": "bun run --cwd frontend lint",
    "build": "bun run --cwd backend build && bun run --cwd frontend build",
    "typecheck": "bun run --cwd backend typecheck && bun run --cwd frontend typecheck",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test --ui",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:record": "PW_RECORDING=1 PLAYWRIGHT_SLOW_MO=1000 playwright test --headed --workers=1 --project=chromium --project=firefox --project=mobile-chrome",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "concurrently": "^9.2.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  }
}
